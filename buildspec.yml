version: 0.2

phases:
  install:
    commands:
      - echo "Skipping Terraform installation. Using pre-installed Terraform..."
      - terraform version

  build:
    commands:
      - echo "Starting Terraform deployment..."
      - cd ./terraform
      - terraform init
      - terraform apply -auto-approve
